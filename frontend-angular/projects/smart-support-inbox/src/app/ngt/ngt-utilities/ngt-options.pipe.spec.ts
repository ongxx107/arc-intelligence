import { NgtOptionsPipe } from './ngt-options.pipe';

describe('NgtOptionsPipe', () => {
  it('create an instance', () => {
    const pipe = new NgtOptionsPipe();
    expect(pipe).toBeTruthy();
  });
});
